/* const hero = document.querySelector('.hero');
const wraith = document.querySelector('.wraith');

const nowWraith = getComputedStyle(wraith);
const nowHero = getComputedStyle(hero);

const enemyDown = () => {
    of (parseInt(nowHero.left) < )
} */